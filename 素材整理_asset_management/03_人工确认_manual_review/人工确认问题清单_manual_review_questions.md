# 人工确认问题清单

状态：待用户确认

## 本轮结论

现在不能进入首条成片 Beta，必须先确认以下 5 个问题：当前样本不是完整直播录屏，33 个 txt 未检测到可用 timecode，且仍有缺 txt、缺视频、配对不确定和业务事实缺口。

## 必须确认的问题

### 1. 是否有完整直播录屏或可用带时间码转写

- 为什么要确认：上一轮扫描目录是 `剪辑解析数据/正样本成片` 和 `剪辑解析数据/负样本成片`，不能证明完整直播录屏已进入当前执行样本。
- 涉及文件 / pair_id / source_id：`素材整理_asset_management/02_素材清单_manifest/缺失与异常清单_missing_or_conflict_report.md`；`素材整理_asset_management/02_素材清单_manifest/素材总清单_asset_inventory.csv`；当前 34 组 `pair_id`。
- 当前证据：缺失与异常清单写明“目录内容是正/负样本成片与 txt，不是完整直播录屏”；上一轮执行报告同样标记为 `部分成立`。
- 不确认会卡住：首条成片 Beta 的输入范围，不能把正/负样本成片写成完整直播录屏解析通过。
- 用户需要回答：本轮是否继续只用这些正/负样本成片做样本级 Beta，还是先补入完整直播录屏或完整直播转写？
- 建议处理：优先补完整直播录屏或带时间码转写；如果暂时没有，明确“只做样本级候选”，不要写成完整直播成片。

### 2. 33 个 txt 是否统一补 timecode

- 为什么要确认：本轮只读检测 33 个业务 txt，`has_timecode=0`，无法定位片段起止点。
- 涉及文件 / pair_id / source_id：`素材整理_asset_management/03_人工确认_manual_review/txt时间码抽检结果_timecode_probe.csv`；`pair_0001` 到 `pair_0034` 中所有有 txt 的 33 个 `source_id`。
- 当前证据：30 个 txt 为 `no_timecode_found`，3 个 txt 为 `empty_txt`；未发现 `00:00`、`00:00:00`、`start_time`、`end_time`、`开始时间`、`结束时间`。
- 不确认会卡住：txt 内容解析、片段边界切分、五结构候选片段筛选和首条成片 Beta。
- 用户需要回答：这些 txt 是否需要先补时间码，还是提供另一批已带 timecode 的转写文件？
- 建议处理：先补 timecode 或重新导出带 `start_time/end_time` 的转写；未补前只能写 `待补时间码`。

### 3. 缺失文件项是否补齐或排除

- 为什么要确认：当前有 1 个视频缺 txt、1 个 txt 缺视频，其中缺视频的 txt 还是空文件。
- 涉及文件 / pair_id / source_id：`pair_0033` / `src_scan_0063` / `会跳舞的臀你想拥有吗？.mp4` 缺 txt；`pair_0034` / `src_scan_0066` / `附件.txt` 缺视频且为空 txt。
- 当前证据：`缺失与异常清单_missing_or_conflict_report.md` 记录缺 txt 和缺视频；timecode probe 记录 `附件.txt` 为 `empty_txt`。
- 不确认会卡住：素材配对闭环和首条成片 Beta 候选池清理。
- 用户需要回答：`pair_0033` 是否补对应 txt 或排除该视频？`pair_0034` 的 `附件.txt` 是否有对应视频，还是应作为无效文本排除？
- 建议处理：补齐缺失文件；若无法补齐，人工确认排除这两项，不进入候选池。

### 4. `pair_0032` 是否确认配对成立

- 为什么要确认：视频和 txt 文件名相似但不完全一致，不能把文件级相似写成内容对应已确认。
- 涉及文件 / pair_id / source_id：`pair_0032`；视频 `src_scan_0004` / `30岁的女性练八爪鱼要多久.mp4`；文本 `src_scan_0003` / `30岁女性八爪鱼要多久.txt`。
- 当前证据：配对表 `pair_confidence=0.92`，`pair_status=待人工确认`，原因是“文件名相似但不完全一致”。
- 不确认会卡住：该素材是否进入首条成片 Beta 候选池，以及后续片段解析是否能引用这组 pair。
- 用户需要回答：这两个文件是否确认为同一主题、同一素材的配对？
- 建议处理：人工打开 txt 和视频核对主题；确认前保持 `待人工确认`，不要改成已配对。

### 5. 转化片段所需业务事实由谁确认

- 为什么要确认：课程名、产品名、价格、权益、退款、效果边界仍是字段缺口，不能从文件名或短 txt 里猜。
- 涉及文件 / pair_id / source_id：`素材整理_asset_management/02_素材清单_manifest/解析不明与字段缺口清单_parse_unclear_gap_report.md`；后续可能影响所有 `conversion_push` 候选片段。
- 当前证据：解析不明清单将课程名、产品名、价格、权益、退款、效果边界标为 `待客户确认`。
- 不确认会卡住：`conversion_push` 促单转化片段判断、合规复核和最终口播事实边界。
- 用户需要回答：这些业务事实由谁给最终口径？是否先提供课程/产品/价格/权益/退款/效果边界说明？
- 建议处理：在进入转化片段筛选前，由客户或用户提供一份确认口径；没有口径时，转化段只能标 `待验证`。

## 暂不处理的问题

- 五结构类型判断：需要内容解析和 timecode，当前不做。
- 高风险表达复核：需要人工听审和客户确认，当前只列入后续。
- 说话人、评论区、课程卡 / 福利卡信息：文件级扫描不可见，当前不做推断。
- 视频内容是否真实对应 txt：本轮未读取视频内容，不能确认。

## 下一步

用户确认后，下一步才能进入 txt 内容解析 / timecode 补齐 / 首条成片 Beta 候选片段筛选。
