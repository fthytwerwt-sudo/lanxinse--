# 解析不明与字段缺口清单

状态：`待验证`
解析不明项数量：12

## 1. P0 必须先解决

- `缺完整直播录屏确认`：当前只扫描到正/负样本成片目录，不能证明完整直播录屏已进入当前执行样本。
- `txt timecode 待验证`：本轮只做文件级扫描，未读取 txt 内容，无法确认是否有 start_time / end_time。
- `业务事实缺口`：课程名、产品名、价格、权益、退款、效果边界均未在素材文件级扫描中确认。
- `视频缺文本`：1 个视频未找到确定 txt。
- `文本缺视频`：1 个 txt 未找到确定视频。
- `配对待人工确认`：1 组配对不能写成已配对。

## 2. P1 建议尽快解决

- `五结构类型待判断`：文件名不能判断片段属于 traffic_hook / course_teaching / objection_response / case_trust / conversion_push。
- `高风险表达待复核`：涉及课程、身体、效果、案例、转化表达时必须人工听审和客户确认。
- `说话人待验证`：文件级扫描无法判断说话人、主播角色或评论区信息。
- `课程卡 / 福利卡信息缺失`：当前文件名不能确认课程卡、商品卡、福利卡是否存在或准确。

## 3. P2 后续补充

- `样本目录属性待确认`：正/负样本成片可作为参考样本，但不等同于完整直播素材。
- `后续需要人工听审`：进入候选片段前应抽检音频清晰度、转写质量和上下文完整性。

## 4. 字段缺口逐项检查

- `source_id`：`已建立`。素材总清单已生成 source_id。
- `timecode`：`待验证`。未读取 txt 内容，不能确认时间码。
- `start_time / end_time`：`待验证`。需要进入转写内容检查。
- `视频主题`：`部分成立`。只能从文件名初步猜测。
- `视频和 txt 配对`：`部分成立`。同名可作为候选，待人工抽检。
- `课程名 / 产品名`：`待客户确认`。文件名不足以确认。
- `价格 / 权益 / 退款 / 效果边界`：`待客户确认`。不得从素材名猜测。
- `评论区信息`：`待验证`。文件级扫描不可见。
- `课程卡 / 福利卡信息`：`待验证`。文件级扫描不可见。
- `低置信度转写`：`待验证`。尚未读取转写内容。
- `说话人`：`待验证`。尚未听审或解析。
- `高风险表达`：`待验证`。需要人工听审和客户确认。
- `五结构类型`：`待验证`。文件名不能替代内容判断。
- `unmapped_clip`：`待创建`。后续片段级解析时建立。
- `人工听审`：`待创建`。进入候选片段前需要。
- `客户确认`：`待客户确认`。业务事实类字段必须客户确认。
